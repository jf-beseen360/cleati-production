services:
  - type: web
    name: cleati-v3
    env: python
    plan: free
    buildCommand: pip install -r requirements-minimal.txt
    startCommand: python -m uvicorn cleati_production_api_v3_i18n:app --host 0.0.0.0 --port $PORT
